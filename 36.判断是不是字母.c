//#define _CRT_SECURE_NO_WARNINGS 
//
//#include <stdio.h>
//
////int main()
////{
////	char ch = 0;
////	while (scanf("%c",&ch) == 1)
////	{
////		if ((ch >= 'a' && ch <= 'z') || (ch >= 'A' && ch <= 'Z'))
////			printf("%c is an alphabet.\n", ch);
////		else
////			printf("%c is not an alphabet.\n", ch);
////		getchar();
////	}
////	return 0;
////}
//
//int main()
//{
//	char ch = 0;
//	while (scanf(" %c", &ch) == 1)//在%c前面夹空格,表示跳过下一个字符前所有的空白字符
//	{
//		if ((ch >= 'a' && ch <= 'z') || (ch >= 'A' && ch <= 'Z'))
//			printf("%c is an alphabet.\n", ch);
//		else
//			printf("%c is not an alphabet.\n", ch);
//	}
//	return 0;
//}

//注:判断是否为字母的库函数 isalpha()
//头文件:<ctype.h>
